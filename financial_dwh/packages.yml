# dbt-utils para los tests dbt_utils.unique_combination_of_columns y
# dbt_utils.accepted_range usados en _models.yml.
# Después de un git pull correr: dbt deps
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
