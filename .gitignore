# Virtual environment
.venv/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
*claude.md
*transition_bible.json
*vps_info.md
*response.json
*inspect_rds.py

# Build / packaging artifacts
financial_data_etl.egg-info/
build/
dist/
ignorar/
nginx_vps_info/

# Transient PR body scratch files (used with `gh pr create --body-file`)
.github/pr_body_*.md
# Local runtime artifacts
logs/
ws_traces/
.claude/

# Local databases
*.db
*.sqlite
*.sqlite3
.env
*.snapshot
private_config.json

# OS noise
.DS_Store
Thumbs.db

*.zip

# GCP Service Account keys — NEVER commit, always via Secrets Manager
*-sa-key.json
gcp/**/*-key.json

# Local secret outputs from infra setup scripts — NEVER commit
.secrets-out/

# DBT artifacts — regenerados por `dbt deps` / `dbt run`, no van al repo
financial_dwh/target/
financial_dwh/dbt_packages/
financial_dwh/logs/